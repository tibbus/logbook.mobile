import React from 'react';
import {
  StyleSheet,
} from 'react-native';
import palette from '../../Themes/palette';

export default StyleSheet.create({
  /* ====== General styles ====== */
  gradient: {
    flex: 1,
  } as React.ViewStyle,
});